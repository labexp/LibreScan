class Project:

    def __init__(self, p_id, p_name, p_description, p_lang, p_cam_config, p_output_formats):
        self.id = p_id
        self.name = p_name
        self.description = p_description
        self.lang = p_lang
        self.cam_config = p_cam_config
        self.output_formats = p_output_formats
